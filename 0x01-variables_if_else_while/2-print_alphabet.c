#include <stdio.h>

/**
 * main - print alpha
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
char amine;

for (amine = 'a'; amine <= 'z'; amine++)
	putchar(amine);

putchar('\n');

return (0);
}
